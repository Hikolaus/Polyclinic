@model List<ClinicApp.Models.DoctorModels.Doctor>
@{
    ViewData["Title"] = "Управление расписанием";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="fas fa-calendar-check me-2"></i>Расписания врачей</h2>
        <a href="@Url.Action("BulkSchedule", "Admin")" class="btn btn-success shadow-sm">
            <i class="fas fa-magic me-1"></i> Генератор расписания
        </a>
    </div>

    <div class="alert alert-info shadow-sm mb-4">
        <i class="fas fa-info-circle me-2"></i>
        Здесь вы можете видеть статус расписания каждого врача и переходить к детальной настройке слотов.
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body p-0">
            <table class="table table-hover mb-0 align-middle">
                <thead class="table-light">
                    <tr>
                        <th style="width: 30%">ФИО Врача</th>
                        <th style="width: 25%">Специализация</th>
                        <th style="width: 25%">Статус графика</th>
                        <th style="width: 20%" class="text-end">Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var doc in Model)
                        {
                            int activeSlots = doc.Schedules?.Count(s => s.IsActive) ?? 0;
                            bool hasSchedule = activeSlots > 0;

                            <tr>
                                <td>
                                    <div class="fw-bold">@(doc.User?.FullName ?? "Нет данных")</div>
                                    <small class="text-muted">Лицензия: @doc.LicenseNumber</small>
                                </td>
                                <td>
                                    <span class="badge bg-light text-dark border">
                                        @(doc.Specialization?.Name ?? "Без спец.")
                                    </span>
                                </td>
                                <td>
                                    @if (hasSchedule)
                                    {
                                        <span class="text-success fw-bold">
                                            <i class="fas fa-check-circle me-1"></i> Заполнено
                                        </span>
                                        <div class="small text-muted">(@activeSlots рабочих интервалов)</div>
                                    }
                                    else
                                    {
                                        <span class="text-danger fw-bold">
                                            <i class="fas fa-times-circle me-1"></i> Пусто
                                        </span>
                                    }
                                </td>
                                <td class="text-end">
                                    <a href="@Url.Action("EditSchedule", "Admin", new { doctorId = doc.Id })"
                                       class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-cog me-1"></i> Настроить
                                    </a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="4" class="text-center text-muted p-5">
                                <i class="fas fa-user-slash fa-2x mb-3"></i>
                                <p>В системе пока нет врачей.</p>
                                <a href="/Admin/RegisterDoctor" class="btn btn-sm btn-primary">Добавить врача</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>